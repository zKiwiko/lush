# API

Lush exposes global Lua modules:

- `lush` for task registration
- `fmt` for formatting helpers
- `sys` for system/shell helpers
- `json` for JSON conversion and I/O
- `build` for C-family build pipelines
